# Core dependencies
faster-whisper>=1.0.0
deep-translator>=1.11.4
torch>=2.0.0
torchaudio>=2.0.0

# Optional GPU support (uncomment if you have NVIDIA GPU with CUDA)
# Install PyTorch with CUDA from: https://pytorch.org/get-started/locally/

# Utility dependencies
tqdm>=4.65.0
colorama>=0.4.6
pydub>=0.25.1

# Persian RTL shaping (optional but recommended)
arabic-reshaper>=3.0.0
python-bidi>=0.6.0
